function sinc,x

;this is the Sinc function.. that is sinc(x)=sin(x)/x

;
y=float(x)

index=where(float(x) eq 0.0)
if (index(0) ne -1) then y(index)=.000001       

return,sin(float(y))/float(y)
end

