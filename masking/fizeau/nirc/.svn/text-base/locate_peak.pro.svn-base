pro Locate_peak, image, x_peak, y_peak, maxim, minim

;Frank Varosi STX @ NASA/GSFC 1991.

	maxim = max( image, imax, MIN=minim )
	s = size( image )
	x_peak = imax MOD s(1)
	y_peak = imax/s(1)
return
end
